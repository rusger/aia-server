# Astrolog API Server Environment Variables
# Copy this file to .env and update with your values

# Server Configuration
# PORT is set to 8080 in systemd service (Caddy handles 443)
# PORT=8080

# TLS/HTTPS Configuration (handled by Caddy)
# USE_HTTPS=false
# TLS_CERT_FILE=/home/ruslan/aia/server/server.crt
# TLS_KEY_FILE=/home/ruslan/aia/server/server.key

# OpenAI API Key (required for ChatGPT proxy)
OPENAI_API_KEY=your-openai-api-key-here

# SMTP Configuration for Email Authentication (GoDaddy M365)
# Required for email login functionality
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=noreply@astrolytix.com
SMTP_PASSWORD=your-email-password-here
SMTP_FROM=Astrolytix <noreply@astrolytix.com>

# Security (optional - auto-generated if not set)
# ASTROLOG_SECRET_KEY=your-secret-key-here

# JWT Secret (auto-generated and stored in jwt_secret.key)
# Do not set this manually unless rotating keys

# Admin Secret Key (REQUIRED for admin subscription management)
# Generate with: openssl rand -hex 32
# This key is used along with email verification for 2FA admin access
ADMIN_SECRET_KEY=your-admin-secret-key-here
